<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="{{ asset('styles/style.css') }}" />
        <title>{% block title %} Author list {% endblock %}</title>
        <h2>{% block Header %} Author list {% endblock %}</h2>
    </head>
    <body>

        <a href="{{ path('app_home_page') }}">back</a>
        {{ include('tables/key_data_table.html.twig', { 'names': names, 'data': data }) }}
    </body>
</html>